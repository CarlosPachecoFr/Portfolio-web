<section id="inicio" class="relative overflow-hidden scroll-mt-100">

   <div class="absolute inset-0 -z-10">
    <!-- Línea 1 (izquierda, arriba) -->
    <div class="absolute top-1 left-4 lg:left-10">
      <div class="w-1.5 h-40 bg-[#33CE33] opacity-30 animate-lineFloat"></div>
    </div>

    <!-- Línea 2 (izquierda, más abajo y a la derecha que la 1) -->
    <div class="absolute top-8/12 left-3 lg:left-110">
      <div class="w-1 h-20 bg-[#33CE33] opacity-30 animate-lineFloat"></div>
    </div>

    <!-- Línea 3 (derecha) -->
    <div class="absolute top-1/3 right-4 lg:right-40">
      <div class="w-1.5 h-32 sm:h-30 bg-[#33CE33] opacity-30 animate-lineFloat"></div>
    </div>
  </div>

  <div class="flex flex-col items-center justify-center text-center gap-6 mt-16 mb-24 md:mb-45 md:mt-20 mx-6">
    <div>
      <p class="text-[#33CE33] text-4xl sm:text-6xl md:text-[95px] font-bold drop-shadow-[0_0_8px_rgba(51,206,51,0.6)] [animation:bounceUp_4s_ease-in-out_infinite]">
        &#123;Carlos.Pacheco&#125;
      </p>
      <div class="border-b-4 border-b-[#33CE33] mx-auto w-40 sm:w-72 mt-2"></div>
    </div>
    <div>
      <p class="text-gray-500 text-base sm:text-xl">
        passion: <span class="text-[#33CE33]">&#34;coding&#34;</span>
      </p>
    </div>
    <div>
      <p class="font-bold text-3xl md:text-[60px] text-gray-800">
        Desarrollador <span class="text-[#33CE33]">Full Stack</span>
      </p>
      <p class="text-gray-500 text-sm sm:text-lg mt-2 leading-relaxed">
        Apasionado por la <span class="text-[#33CE33] font-semibold">tecnología</span>, me gusta crear <span class="text-[#33CE33] font-semibold">experiencias digitales modernas</span> y <span class="text-[#33CE33] font-semibold">soluciones que aporten verdadero valor</span>.
      </p>
    </div>
    <div class="flex flex-col sm:flex-row gap-4 mt-6 w-full sm:w-auto">
      <a href="#proyectos" class="bg-[#33CE33] w-full sm:w-auto cursor-pointer text-white py-2 px-6 sm:px-8 text-base sm:text-lg font-semibold rounded-lg text-center hover:scale-[1.05] ease-in-out duration-300 hover:bg-[#41e841]">
        Ver proyectos
      </a>
      <a href="#contacto" class="border border-[#33CE33] w-full sm:w-auto cursor-pointer text-[#33CE33] py-2 px-6 sm:px-8 text-base sm:text-lg font-semibold rounded-lg text-center hover:scale-[1.05] ease-in-out duration-300 hover:bg-[#33CE33] hover:text-white">
        Contactar
      </a>
    </div>
  </div>
</section>

<style> 
    @keyframes bounceUp { 
        0%, 100% { 
            transform: scale(1) translateY(0); 
        } 
        50% { transform: scale(1.05) translateY(-6px); 
        } 
    }
    /* Movimiento vertical de las líneas */
    @keyframes lineFloat {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
    }

    .animate-lineFloat {
      animation: lineFloat 6s ease-in-out infinite;
    }
</style>